<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.taskapp.mapper.RoleMapper">
    <resultMap id="RoleMap" type="com.example.taskapp.model.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <collection property="permissions" column="id" select="com.example.taskapp.mapper.PermissionMapper.findByRoleId"/>
    </resultMap>

    <select id="findAll" resultMap="RoleMap">
        SELECT * FROM roles
    </select>
    <select id="findById" resultMap="RoleMap">
        SELECT * FROM roles WHERE id = #{id}
    </select>
    <select id="findByUserId" resultMap="RoleMap">
        SELECT r.* FROM roles r
        JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name) VALUES (#{name})
    </insert>
    <update id="update">
        UPDATE roles SET name = #{name} WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM roles WHERE id = #{id}
    </delete>
    <insert id="addPermissionToRole">
        INSERT INTO role_permissions (role_id, permission_id) VALUES (#{roleId}, #{permissionId})
    </insert>
    <delete id="removePermissionFromRole">
        DELETE FROM role_permissions WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </delete>
</mapper>
